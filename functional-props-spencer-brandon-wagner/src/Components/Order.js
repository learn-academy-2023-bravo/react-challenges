
    const Order = (selectedItem, menu, setMenu) => {
        // {menu.map((item, index) => {
        //     return  key={index}>
        //       {item.name}
        //       {item.price}
        //   })}

    }









export default Order